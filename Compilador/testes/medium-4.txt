int a;
int b;
int c;
int f[5];

int seguro(){
    return(f[3]+c);
}

int supA(int a){
    return(a);
}

int supB(int a, int b){
    while(a > b){
        int val;
        val = a;
        a = b - 1;
        b = 2 * val;
    }

    return(a + b);
}

void main(void)
{
    int x;
    int y;

    x = seguro() + supA(a);
    y = 7;
    y = x + seguro();
    x = seguro() + supB(a, b);

    while(x < y){
        int dado;
        int cado;

        cado = dado;
    }

    output(c);
    output(f[2]);

    return;
}